if(NOT EMSCRIPTEN)
  add_subdirectory(SDL)
  add_subdirectory(Exporter)
  if(${VGG_VAR_PLATFORM_TARGET} IN_LIST VGG_LINUX_TARGET_LIST)
    add_subdirectory(EGL)
  endif()
else()
  add_subdirectory(WASM)
endif()
